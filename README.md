
# 🧩 Block Blast  
**Классическая головоломка, оживающая с Qt5 и C++!**
![BlockBlast](https://github.com/Dedushka-CPU/Block_Blast/blob/main/resources/images/icon.jpg)
---

## 🌟 Обзор проекта  
**Block Blast** — увлекательная головоломка, созданная на **C++** с использованием **Qt5** для современного графического интерфейса.  

Размещайте блоки разных форм на сетке **12x12**, чтобы очистить ряды, столбцы или диагонали. Блоки **нельзя вращать**, что делает каждый ход настоящим испытанием для вашего стратегического мышления!  

---

## 🎮 Особенности  
- 🧩 **Интерактивный геймплей**: Перетаскивайте блоки и очищайте линии!  
- 🛠 **Разнообразие форм**: Уникальные блоки добавляют глубину и сложность.  
- 📊 **Система очков**: Следите за прогрессом и бейте рекорды!  
- 🎨 **Красивый интерфейс**: Графика на Qt5 радует глаз.  
- 🚀 **Кроссплатформенность**: Работает на Windows, Linux и macOS(не точно,не проверяли).  

---

## 🛠 Требования  
- 🔧 **Qt5** (установленный и настроенный).  
- 💻 **Компилятор C++** (GCC, MSVC или Clang).  
- 📦 **Qt Creator** (рекомендуется для удобной разработки).  

---

## 📥 Установка и запуск  

### Windows  
1. Скачайте готовый `.exe` из [релизов](https://github.com/Dedushka-CPU/Block_Blast/releases).  
2. Запустите файл и наслаждайтесь игрой!  

### Linux  
1. Установите зависимости:  
   ```bash
   sudo apt update
   sudo apt install qtbase5-dev qt5-make
   sudo apt install qtmultimedia5-dev
   ```
2. Склонируйте репозиторий:  
   ```bash
   git clone https://github.com/Dedushka-CPU/Block_Blast
   ```
3. Откройте `BlockBlast.pro` в Qt Creator и соберите проект.  
4. Запустите.  

---

## 📂 Структура проекта  
```
BlockBlast/
├── headers/                    # Заголовочные файлы
│   ├── blockshape.h           # Классы для форм блоков
│   ├── fallingshapeswidget.h  # Падающие фигуры на фоне
│   ├── gameboard.h            # Игровое поле
│   ├── mainwindow.h           # Основное окно
│   └── shapewidget.h          # Виджет форм
├── source/                     # Исходный код
│   ├── blockshape.cpp         
│   ├── fallingshapeswidget.cpp 
│   ├── gameboard.cpp          
│   ├── mainwindow.cpp         
│   └── shapewidget.cpp        
├── resources/                  # Ресурсы
│   ├── images/                # Изображения
│   └── sounds/                # Звуки
├── main.cpp                    # Точка входа
├── BlockBlast.pro              # Файл сборки qmake
└── README.md                   # Документация
```

---

## 🎲 Как играть  
1. 🖱 Перетаскивайте блоки на сетку 12x12.  
2. 🧠 Заполняйте ряды, столбцы или диагонали, чтобы очистить их.  
3. 🚫 Блоки нельзя вращать — планируйте ходы заранее!  
4. 🏆 Набирайте очки и соревнуйтесь с друзьями!  

Если блок не помещается, игра завершится. Ошибки отображаются в интерфейсе.  

---

## 🚀 Планы на будущее  
- 🎨 Темы оформления интерфейса.  
- 📱 Поддержка сенсорного ввода для мобильных устройств.  
- ⚙ Уровни сложности и новые формы блоков.  

---

**Block Blast** — это вызов вашему уму!  
*Создано с ❤️ для любителей головоломок!*  

---
## Компоненты игры

Ниже подробно описаны ключевые классы, составляющие игру **BlockBlast**, их назначение, используемые библиотеки Qt, причины их выбора, а также важные методы и их функциональность.

### 1. BlockShape

#### Назначение
Класс `BlockShape` представляет фигуру в игре, состоящую из набора клеток, цвета и уникального идентификатора. Используется для хранения данных о фигурах, которые игрок размещает на поле, а также для передачи данных через механизм drag-and-drop.

#### Используемые библиотеки Qt и их назначение
- **`QVector`** (`QtCore`):
  - **Зачем**: Хранит список клеток (`Cell`) фигуры.
  - **Почему**: `QVector` — это динамический массив с оптимизированной производительностью для работы с данными в Qt. Он обеспечивает удобный доступ к элементам и эффективное управление памятью.
  - **Пример использования**: `QVector<Cell> cells` для хранения координат клеток.
- **`QString`** (`QtCore`):
  - **Зачем**: Хранит цвет фигуры в формате HEX (например, "#ff6b6b").
  - **Почему**: `QString` поддерживает Unicode и удобен для работы со строками в Qt, особенно для стилей CSS в `QStyleSheet`.
  - **Пример использования**: `QString color` для задания цвета фигуры.
- **`QDataStream`** (`QtCore`):
  - **Зачем**: Используется для сериализации и десериализации данных фигуры при drag-and-drop.
  - **Почему**: `QDataStream` предоставляет удобный и надёжный способ передачи сложных данных (структур, списков) между компонентами Qt.
  - **Пример использования**: Операторы `<<` и `>>` для записи/чтения `Cell` в поток.

#### Ключевые элементы
- **Структура `Cell`**:
  - Хранит координаты клетки (`int row`, `int col`).
  - Используется для описания формы фигуры относительно начальной позиции.
- **Поля**:
  - `QVector<Cell> cells`: Список клеток, определяющих форму.
  - `QString color`: Цвет фигуры.
  - `int id`: Уникальный идентификатор для отслеживания фигуры.
- **Конструктор**:
  ```cpp
  BlockShape(const QVector<Cell> &c = {}, const QString &clr = "", int i = 0);
  ```
  - Инициализирует фигуру с заданными клетками, цветом и ID.
  - Параметры по умолчанию позволяют создавать пустую фигуру.
- **Операторы сериализации**:
  ```cpp
  QDataStream &operator<<(QDataStream &out, const BlockShape::Cell &cell);
  QDataStream &operator>>(QDataStream &in, BlockShape::Cell &cell);
  ```
  - Записывают и читают координаты клетки в/из потока `QDataStream`.
  - Используются в `ShapeWidget` и `GameBoard` для передачи данных при перетаскивании.

#### Пример функциональности
- Создание фигуры (например, квадрат):
  ```cpp
  QVector<BlockShape::Cell> cells = {{0,0}, {0,1}, {1,0}, {1,1}};
  BlockShape square(cells, "#ff6b6b", 1);
  ```
- Сериализация клетки:
  ```cpp
  QDataStream stream(&data, QIODevice::WriteOnly);
  BlockShape::Cell cell{2, 3};
  stream << cell; // Записывает row=2, col=3
  ```

#### Почему важен
`BlockShape` — основа для представления всех игровых фигур. Его простая структура и поддержка сериализации обеспечивают гибкость и надёжность при передаче данных между компонентами.

---

### 2. FallingShapesWidget

#### Назначение
Класс `FallingShapesWidget` отвечает за анимацию падающих фигур на заднем плане всех экранов (загрузка, меню, игра). Это декоративный элемент, создающий визуальную динамику.

#### Используемые библиотеки Qt и их назначение
- **`QWidget`** (`QtWidgets`):
  - **Зачем**: Базовый класс для создания виджета, отображающего фигуры.
  - **Почему**: `QWidget` — основа для всех визуальных компонентов Qt, поддерживает события рисования и изменения размера.
  - **Пример использования**: `FallingShapesWidget` наследуется от `QWidget`.
- **`QPainter`** (`QtGui`):
  - **Зачем**: Рисует фигуры на виджете.
  - **Почему**: `QPainter` предоставляет мощный API для 2D-графики, включая рисование прямоугольников, применение трансформаций и сглаживание.
  - **Пример использования**: `paintEvent` для отрисовки клеток фигур.
- **`QTransform`** (`QtGui`):
  - **Зачем**: Применяет трансформации (смещение, вращение) к фигурам.
  - **Почему**: `QTransform` позволяет легко управлять положением и ориентацией объектов, что необходимо для анимации вращения.
  - **Пример использования**: Вращение фигур в `paintEvent`.
- **`QRandomGenerator`** (`QtCore`):
  - **Зачем**: Генерирует случайные параметры фигур (форма, позиция, скорость, угол).
  - **Почему**: `QRandomGenerator` — современный и надёжный генератор случайных чисел в Qt.
  - **Пример использования**: `resetShapes` для выбора случайного шаблона и цвета.
- **`QTimerEvent`** (`QtCore`):
  - **Зачем**: Обновляет позиции фигур с частотой ~60 FPS.
  - **Почему**: `QTimerEvent` позволяет создавать анимации, вызывая обновления через равные промежутки времени.
  - **Пример использования**: `timerEvent` для перемещения и вращения фигур.

#### Ключевые элементы
- **Структура `Shape`**:
  - Хранит данные о фигуре: `QVector<QPoint> cells` (координаты клеток), `double x, y` (позиция), `double speed` (скорость падения), `double rotation` (угол поворота), `QString color` (цвет).
- **Поля**:
  - `QVector<Shape> shapes`: Список из 10 фигур.
  - `int timerId`: Идентификатор таймера для анимации.
- **Методы**:
  - **Конструктор**:
    ```cpp
    explicit FallingShapesWidget(QWidget *parent = nullptr);
    ```
    - Инициализирует виджет, создаёт 10 фигур и запускает таймер (16 мс).
  - **resetShapes**:
    ```cpp
    void resetShapes(const QVector<QVector<QPoint>> &templates, const QStringList &colors);
    ```
    - Сбрасывает параметры фигур (форма, позиция, скорость, угол, цвет).
    - Используется при создании и изменении размера окна.
  - **paintEvent**:
    ```cpp
    void paintEvent(QPaintEvent *event) override;
    ```
    - Рисует фигуры с учётом их позиции и вращения.
  - **timerEvent**:
    ```cpp
    void timerEvent(QTimerEvent *event) override;
    ```
    - Обновляет позиции и углы фигур, сбрасывает их при выходе за экран.
  - **resizeEvent**:
    ```cpp
    void resizeEvent(QResizeEvent *event) override;
    ```
    - Пересоздаёт фигуры при изменении размера окна, чтобы они оставались в пределах виджета.

#### Пример функциональности
- Создание фигуры:
  ```cpp
  Shape shape;
  shape.cells = {{0,0}, {0,1}}; // Две клетки
  shape.x = 100;
  shape.y = -40;
  shape.speed = 1.2;
  shape.rotation = 45;
  shape.color = "#ff6b6b";
  ```
- Отрисовка:
  ```cpp
  QPainter painter(this);
  QTransform transform;
  transform.translate(shape.x, shape.y);
  transform.rotate(shape.rotation);
  painter.setTransform(transform);
  painter.setBrush(QColor(shape.color));
  painter.drawRect(0, 0, 18, 18); // Клетка 20x20 с отступом
  ```

#### Почему важен
`FallingShapesWidget` создаёт визуальную привлекательность игры, делая интерфейс живым и динамичным. Использование `QPainter` и `QTransform` позволяет эффективно анимировать фигуры без значительных затрат ресурсов.

---

### 3. GameBoard

#### Назначение
Класс `GameBoard` представляет игровое поле 12x12 клеток, где игрок размещает фигуры. Он обрабатывает drag-and-drop, проверяет возможность размещения, очищает заполненные линии и обновляет счёт.

#### Используемые библиотеки Qt и их назначение
- **`QWidget`** (`QtWidgets`):
  - **Зачем**: Основа для игрового поля.
  - **Почему**: Поддерживает события мыши, drag-and-drop и управление дочерними виджетами.
  - **Пример использования**: `GameBoard` наследуется от `QWidget`.
- **`QGridLayout`** (`QtWidgets`):
  - **Зачем**: Организует клетки поля в сетке 12x12.
  - **Почему**: `QGridLayout` идеально подходит для создания регулярной сетки с минимальными усилиями.
  - **Пример использования**: `gridLayout` для размещения `QLabel`.
- **`QLabel`** (`QtWidgets`):
  - **Зачем**: Отображает каждую клетку поля.
  - **Почему**: `QLabel` прост в использовании для отображения цветных квадратов с поддержкой стилей.
  - **Пример использования**: `cells[12][12]` для визуализации поля.
- **`QDragEnterEvent`, `QDragMoveEvent`, `QDropEvent`, `QDragLeaveEvent`** (`QtGui`):
  - **Зачем**: Обрабатывают события drag-and-drop.
  - **Почему**: Эти классы предоставляют API для реализации перетаскивания фигур.
  - **Пример использования**: `dragMoveEvent` для подсветки клеток.
- **`QMimeData`** (`QtCore`):
  - **Зачем**: Передаёт данные фигуры при перетаскивании.
  - **Почему**: `QMimeData` поддерживает произвольные форматы данных, такие как `"application/x-blockshape"`.
  - **Пример использования**: Десериализация данных в `dropEvent`.
- **`QTimer`** (`QtCore`):
  - **Зачем**: Управляет анимацией очистки линий.
  - **Почему**: `QTimer` позволяет создавать временные интервалы для анимации (например, мигание клеток).
  - **Пример использования**: `animationTimer` в `animateClearing`.

#### Ключевые элементы
- **Поля**:
  - `QLabel *cells[12][12]`: Массив меток для отображения клеток.
  - `QVector<QVector<bool>> grid`: Логическое состояние поля (занято/свободно).
  - `QGridLayout *gridLayout`: Макет для клеток.
  - `QTimer *animationTimer`: Таймер для анимации.
  - `QVector<QPoint> cellsToClear`: Список клеток для очистки.
  - `int animationStep`: Текущий шаг анимации.
- **Методы**:
  - **Конструктор**:
    ```cpp
    explicit GameBoard(QWidget *parent = nullptr);
    ```
    - Создаёт поле, инициализирует клетки и включает drag-and-drop.
  - **placeShape**:
    ```cpp
    void placeShape(const BlockShape &shape, int row, int col);
    ```
    - Размещает фигуру на поле и проверяет линии для очистки.
  - **canPlaceShape**:
    ```cpp
    bool canPlaceShape(const BlockShape &shape, int row, int col) const;
    ```
    - Проверяет, можно ли разместить фигуру в заданной позиции.
  - **canPlaceAnyShape**:
    ```cpp
    bool canPlaceAnyShape(const BlockShape shapes[], ShapeWidget *shapeWidgets[]) const;
    ```
    - Проверяет, можно ли разместить хотя бы одну из трёх фигур.
  - **clearHighlight**:
    ```cpp
    void clearHighlight();
    ```
    - Снимает подсветку незанятых клеток.
  - **clearBoard**:
    ```cpp
    void clearBoard();
    ```
    - Очищает поле.
  - **checkAndClearLines**:
    ```cpp
    void checkAndClearLines();
    ```
    - Проверяет заполненные линии и запускает анимацию очистки.
  - **animateClearing**:
    ```cpp
    void animateClearing();
    ```
    - Управляет анимацией мигания клеток.
  - **finishClearing**:
    ```cpp
    void finishClearing();
    ```
    - Завершает очистку, обновляет счёт.
- **Сигналы**:
  - `dropReceived(int shapeId, const QPoint &pos)`: Испускается при сбросе фигуры.
  - `scoreUpdated(int points)`: Испускается при начислении очков.

#### Пример функциональности
- Размещение фигуры:
  ```cpp
  BlockShape shape({{0,0}, {0,1}}, "#ff6b6b", 1);
  gameBoard->placeShape(shape, 5, 5); // Размещает фигуру в (5,5)
  ```
- Подсветка при перетаскивании:
  ```cpp
  cells[row][col]->setStyleSheet("background-color: #ff6b6b80; border: 1px solid #3a3a4d;");
  ```

#### Почему важен
`GameBoard` — ядро игровой логики. Он объединяет визуальное представление поля, механику размещения фигур и обработку событий, обеспечивая плавный игровой процесс.

---

### 4. ShapeWidget

#### Назначение
Класс `ShapeWidget` отображает фигуру в области спавна и позволяет игроку перетаскивать её на игровое поле.

#### Используемые библиотеки Qt и их назначение
- **`QWidget`** (`QtWidgets`):
  - **Зачем**: Базовый класс для виджета фигуры.
  - **Почему**: Поддерживает рисование и события мыши.
  - **Пример использования**: `ShapeWidget` наследуется от `QWidget`.
- **`QPainter`** (`QtGui`):
  - **Зачем**: Рисует клетки фигуры.
  - **Почему**: Простой и эффективный способ отрисовки 2D-графики.
  - **Пример использования**: `paintEvent` для отображения фигуры.
- **`QDrag`** (`QtGui`):
  - **Зачем**: Инициирует процесс перетаскивания.
  - **Почему**: `QDrag` — стандартный класс Qt для drag-and-drop.
  - **Пример использования**: `mousePressEvent` для запуска перетаскивания.
- **`QMimeData`** (`QtCore`):
  - **Зачем**: Передаёт данные фигуры при перетаскивании.
  - **Почему**: Поддерживает кастомные форматы данных.
  - **Пример использования**: Сериализация `BlockShape` в `mousePressEvent`.
- **`QMouseEvent`** (`QtGui`):
  - **Зачем**: Обрабатывает нажатия мыши.
  - **Почему**: Необходим для запуска drag-and-drop.
  - **Пример использования**: `mousePressEvent` для реагирования на левую кнопку мыши.

#### Ключевые элементы
- **Поля**:
  - `BlockShape shape`: Данные фигуры.
- **Методы**:
  - **Конструктор**:
    ```cpp
    ShapeWidget(const BlockShape &shape, QWidget *parent = nullptr);
    ```
    - Инициализирует виджет с заданной фигурой, устанавливает размер 250x250.
  - **getShape**:
    ```cpp
    const BlockShape &getShape() const;
    ```
    - Возвращает фигуру.
  - **paintEvent**:
    ```cpp
    void paintEvent(QPaintEvent *event) override;
    ```
    - Рисует фигуру в центре виджета.
  - **mousePressEvent**:
    ```cpp
    void mousePressEvent(QMouseEvent *event) override;
    ```
    - Запускает перетаскивание при нажатии левой кнопкой мыши.

#### Пример функциональности
- Отрисовка фигуры:
  ```cpp
  QPainter painter(this);
  painter.setBrush(QColor("#ff6b6b"));
  painter.drawRect(100, 100, 28, 28); // Клетка 30x30 с отступом
  ```
- Запуск перетаскивания:
  ```cpp
  QDrag *drag = new QDrag(this);
  QMimeData *mimeData = new QMimeData;
  mimeData->setData("application/x-blockshape", data);
  drag->setMimeData(mimeData);
  drag->exec(Qt::MoveAction);
  ```

#### Почему важен
`ShapeWidget` обеспечивает интерактивность, позволяя игроку перетаскивать фигуры. Его интеграция с `QDrag` и `QMimeData` делает процесс перетаскивания надёжным и гибким.

---

### 5. MainWindow

#### Назначение
Класс `MainWindow` управляет главным окном приложения, переключением экранов (загрузка, меню, игра, скример) и общей логикой игры, включая генерацию фигур, обработку сброса и завершение игры.

#### Используемые библиотеки Qt и их назначение
- **`QMainWindow`** (`QtWidgets`):
  - **Зачем**: Основа для главного окна.
  - **Почему**: Поддерживает меню, панели инструментов и центральный виджет, что удобно для сложных интерфейсов.
  - **Пример использования**: `MainWindow` наследуется от `QMainWindow`.
- **`QStackedWidget`** (`QtWidgets`):
  - **Зачем**: Переключает экраны (загрузка, меню, игра, скример).
  - **Почему**: Позволяет легко управлять набором виджетов, показывая только один за раз.
  - **Пример использования**: `stackedWidget` для управления экранами.
- **`QVBoxLayout`, `QHBoxLayout`** (`QtWidgets`):
  - **Зачем**: Организуют элементы интерфейса.
  - **Почему**: Удобны для создания адаптивных макетов.
  - **Пример использования**: `gameLayout` для игрового экрана, `spawnLayout` для фигур.
- **`QLabel`** (`QtWidgets`):
  - **Зачем**: Отображает текст (счёт, заголовки) и изображения (скример).
  - **Почему**: Простота и гибкость в настройке через `QStyleSheet`.
  - **Пример использования**: `scoreLabel` для счёта.
- **`QPushButton`** (`QtWidgets`):
  - **Зачем**: Реализует кнопки (начать игру, выход).
  - **Почему**: Поддерживает сигналы и стилизацию.
  - **Пример использования**: `startButton` в главном меню.
- **`QProgressBar`** (`QtWidgets`):
  - **Зачем**: Показывает прогресс загрузки.
  - **Почему**: Визуально понятный компонент для имитации загрузки.
  - **Пример использования**: `progressBar` на экране загрузки.
- **`QSoundEffect`** (`QtMultimedia`):
  - **Зачем**: Воспроизводит звук скримера.
  - **Почему**: Простой API для воспроизведения звуков без задержек.
  - **Пример использования**: `screamSound` в `endGame`.
- **`QTimer`** (`QtCore`):
  - **Зачем**: Управляет анимацией загрузки и длительностью скримера.
  - **Почему**: Надёжный способ выполнения задач по расписанию.
  - **Пример использования**: `screamerTimer` для возврата в меню.
- **`QGraphicsDropShadowEffect`** (`QtWidgets`):
  - **Зачем**: Добавляет тени к тексту.
  - **Почему**: Улучшает визуальную привлекательность интерфейса.
  - **Пример использования**: Тень для заголовка `BlockBlast`.

#### Ключевые элементы
- **Поля**:
  - `QStackedWidget *stackedWidget`: Управляет экранами.
  - `GameBoard *gameBoard`: Игровое поле.
  - `ShapeWidget *shapeWidgets[3]`: Три фигуры в области спавна.
  - `BlockShape shapes[3]`: Данные фигур.
  - `QHBoxLayout *spawnLayout`: Макет для фигур.
  - `QLabel *scoreLabel`: Метка для счёта.
  - `QSoundEffect *screamSound`: Звук скримера.
  - `QTimer *screamerTimer`: Таймер для скримера.
  - `int nextShapeId`: Счётчик идентификаторов фигур.
  - `int score`: Текущий счёт.
- **Методы**:
  - **Конструктор**:
    ```cpp
    MainWindow(QWidget *parent = nullptr);
    ```
    - Создаёт интерфейс, инициализирует экраны и запускает анимацию загрузки.
  - **startGame**:
    ```cpp
    void startGame();
    ```
    - Очищает поле, сбрасывает счёт, генерирует фигуры и переключает на экран игры.
  - **generateShapes**:
    ```cpp
    void generateShapes();
    ```
    - Создаёт три новые фигуры с случайными формами и цветами.
  - **handleDrop**:
    ```cpp
    void handleDrop(int shapeId, const QPoint &pos);
    ```
    - Обрабатывает сброс фигуры, размещает её, обновляет счёт и проверяет конец игры.
  - **updateScore**:
    ```cpp
    void updateScore(int points);
    ```
    - Добавляет очки и обновляет метку счёта.
  - **endGame**:
    ```cpp
    void endGame();
    ```
    - Запускает скример и возвращает в меню через 4 секунды.
  - **returnToMenu**:
    ```cpp
    void returnToMenu();
    ```
    - Переключает на главное меню и очищает поле.
  - **exitGame**:
    ```cpp
    void exitGame();
    ```
    - Закрывает приложение.

#### Пример функциональности
- Переключение экрана:
  ```cpp
  stackedWidget->setCurrentIndex(2); // Показать экран игры
  ```
- Генерация фигуры:
  ```cpp
  shapes[i] = BlockShape(shapeTemplates[shapeIdx], colors[colorIdx], nextShapeId++);
  shapeWidgets[i] = new ShapeWidget(shapes[i], stackedWidget->widget(2));
  ```

#### Почему важен
`MainWindow` связывает все компоненты игры, управляя интерфейсом и логикой. Его использование `QStackedWidget` и сигналов/слотов обеспечивает модульность и отзывчивость.




---
